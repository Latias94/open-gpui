# Engineering Memory Update Log

## 2026-06-18
* **Initialization**: Created engineering wiki memory bundle.

## 2026-06-19
* **Tear-off source invalidation cleanup**: Collapsed `Missing/Moved` into a single `SourceUnavailable` tear-off cancellation path, so runtime no longer reclassifies the source payload twice before closing a newly opened window.
* **Verification**: `CARGO_TARGET_DIR=target-ce-work cargo check -p open-gpui-docking --lib` and `CARGO_TARGET_DIR=target-ce-work cargo nextest run -p open-gpui-docking viewport_runtime_handle_closes_unregistered_window_when_tear_off_source_closes viewport_runtime_handle_closes_unregistered_window_when_tear_off_source_moves --no-fail-fast` passed.
* **Architecture cleanup**: Moved `global_screen_viewport_hits` from the deleted `viewport_target` thin wrapper onto `DockViewportAdapter`, updated call sites in viewport, placement, host tests, and drop routing, and removed the `mod viewport_target` entry from `lib.rs`.
* **Verification**: `cargo check -p open-gpui-docking --lib` passed. `cargo fmt --all --check` passed after import cleanup. `cargo nextest run -p open-gpui-docking viewport_coordinates viewport_placement host_viewport_tests viewport_drop_route --no-fail-fast` passed (65 tests, 534 skipped).
* **Docking authority cleanup**: Renamed route authority `SingleGeometryHit` to `UniqueGeometryHit` and updated crate docs to remove stale `active-window` arbitration wording. Kept ImGui-like unique-geometry fallback for same-window commits while preserving `SourceOnly` cross-viewport fail-closed behavior.
* **Verification**: `cargo fmt --all --check`, `cargo check -p open-gpui-docking --lib`, and `cargo nextest run -p open-gpui-docking viewport_drop_route host_viewport_runtime_tests --no-fail-fast` passed (131 tests, 468 skipped).
* **Recent focus renaming**: Renamed `last_focused` viewport ordering into `recent_focus` across registry, runtime, adapter, viewport helpers, and tests so the state reads as diagnostic/recovery ordering instead of route authority. Production routing still ignores this state; it is only used for explicit ordering tests and diagnostics.
* **Verification**: `cargo fmt --all` and `cargo check -p open-gpui-docking --lib` passed after the rename.
* **Verification**: `cargo nextest run -p open-gpui-docking host_viewport_runtime_tests viewport_registry viewport_drop_route --no-fail-fast` passed (140 tests, 459 skipped).
* **Hovered-host commit seam cleanup**: Removed the unused `host_space` parameter from hovered-host delivery authorization, so commit authorization now depends only on the resolved target window id and payload. Added a regression test proving that a resolved delivery with `target_window_id = None` still fails closed.
* **Verification**: `cargo check -p open-gpui-docking --lib`, `cargo fmt --all --check`, and `cargo nextest run -p open-gpui-docking hovered_host_release_rejects_delivery_without_target_window_id hovered_host_release_accepts_fresh_delivery_without_preview_token hovered_host_release_rejects_other_host_window hovered_host_release_rejects_source_only_release hovered_host_release_commits_when_host_window_matches_delivery hovered_host_release_reports_payload_mismatch tear_off_delivery_is_not_hovered_host_workspace_authority --no-fail-fast` passed (7 tests, 596 skipped).
* **Pointer authority deepening**: Deleted the shallow `DockViewportPointerAuthority` wrapper and folded its state into `DockViewportPointerReleaseAuthority`, so normalization and coordinate-space data live on the same seam. Updated `DockViewportDropRouteRequest` call sites accordingly.
* **Verification**: `cargo fmt --all --check`, `cargo check -p open-gpui-docking --lib`, and `cargo nextest run -p open-gpui-docking pointer_release_keeps_global_screen_coordinates_when_backend_supports_global_bounds pointer_release_splits_local_coordinate_authority_without_global_bounds pointer_release_strips_trusted_hovered_window_authority_for_no_input_source_without_backend_awareness pointer_release_keeps_trusted_hovered_window_when_no_input_is_not_reported hovered_host_global_drop_authorizes_unique_geometry_without_hover_authority source_only_global_drop_authorizes_source_viewport_unique_geometry drop_route_authorizes_backend_hover_fallback_when_hovered_backend_is_unavailable --no-fail-fast` passed (7 tests, 596 skipped).
* **Runtime registration post-processing cleanup**: Collapsed duplicated regular-open and tear-off-completion viewport registration cleanup into `DockViewportRuntime::register_runtime_viewport`, keeping owned-window tracking, retired-window revival, recent-focus stamping, replaced-window cleanup, and close-plan discard in one runtime path.
* **Verification**: `cargo fmt --all`, `cargo check -p open-gpui-docking --lib`, and `cargo nextest run -p open-gpui-docking viewport_runtime_handle_closes_unregistered_window_when_tear_off_source_closes viewport_runtime_handle_closes_unregistered_window_when_tear_off_source_moves platform_activation_updates_focus_stamp_and_only_mouse_down_suppresses_focus_restore platform_activation_after_destroyed_previous_focused_viewport_does_not_restore_panel_focus runtime_rendered_mouse_up_outside_viewports_tears_off_tab runtime_torn_off_tab_can_dock_back_to_source_viewport --no-fail-fast` passed (6 tests, 598 skipped).
* **Focus activation seam cleanup**: Removed the shallow `DockController::select_tab_and_request_focus()` helper and the host-layer `ViewportActivation` fallback branch. `DockHost` now resolves visible-panel focus directly, reveals hidden tabs explicitly by selecting them, and then requests focus through the live panel registration path.
* **Verification**: `cargo fmt --all`, `cargo check -p open-gpui-docking --lib`, and `cargo nextest run -p open-gpui-docking viewport_panel_request_selects_hidden_tab_before_restoring_focus viewport_activation_restores_recorded_last_focused_panel viewport_activation_clears_failed_panel_focus_request platform_activation_does_not_restore_panel_focus_while_mouse_is_pressed close_recovery_does_not_reveal_hidden_recorded_panel --no-fail-fast` passed (5 tests, 599 skipped).
