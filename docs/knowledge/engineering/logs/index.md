# Memory Events

This directory is for append-only chronological event shards.

Prefer `wiki_memory.py log --root docs\knowledge\engineering --kind "<Kind>" "<message>"` for quick
events. Do not append routine events to the root `log.md`; that file is a historical rollup and may
lag behind the sharded facts in this directory.

